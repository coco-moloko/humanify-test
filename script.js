$(document).ready(function(){
  $('.sliderImage').slick({
    arrows: false,
    dots: true,
    dotsClass:"slick-dots",
    autoplay: true,
    adaptiveHeight: true,
    autoplaySpeed: 10000,
  });
});